using UnityEngine;

public class ExecuteAction : MonoBehaviour
{
    public virtual void Execute(GameObject target, Vector3 origin, GameObject parent = null, int radius = 0)
    {

    }
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
